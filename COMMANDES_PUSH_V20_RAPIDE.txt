=================================================================
   COMMANDES RAPIDES - PUSH CLARAVERSE V20 - 30 AOÛT 2026
=================================================================

REPOSITORY CIBLE:
https://github.com/sekadalle2024/https-github.com-sekadalle2024-v_20_30-08-2026_persistance_table_isolation_chat_OK.git

=================================================================
   MÉTHODE 1: SCRIPT AUTOMATIQUE (RECOMMANDÉ)
=================================================================

# 1. Ouvrir PowerShell dans H:\Claverse_1
cd H:\Claverse_1

# 2. Exécuter le script
.\push-claraverse-v20-30-08-2026.ps1

# C'est tout ! Le script fait tout automatiquement
# Durée: 10-15 minutes
# Pas besoin d'autre commande

=================================================================
   MÉTHODE 2: COMMANDES MANUELLES (SI SCRIPT ÉCHOUE)
=================================================================

# 1. Configuration Git optimale
git config core.compression 0
git config http.postBuffer 1048576000
git config http.lowSpeedTime 999999
git config http.lowSpeedLimit 0
git config pack.windowMemory "100m"
git config pack.packSizeLimit "100m"
git config pack.threads "1"

# 2. Configurer le repository
git remote set-url origin https://github.com/sekadalle2024/https-github.com-sekadalle2024-v_20_30-08-2026_persistance_table_isolation_chat_OK.git

# 3. Vérifier la branche
git branch

# 4. Créer/Changer vers main si nécessaire
git checkout -b main

# 5. Push en 6 parties
git add src/
git commit -m "V20 - 30 Août 2026 - Partie 1: Code Source React/TypeScript"
git push origin main

git add py_backend/
git commit -m "V20 - 30 Août 2026 - Partie 2: Backend Python"
git push origin main

git add public/
git commit -m "V20 - 30 Août 2026 - Partie 3: Fichiers Publics"
git push origin main

git add "Doc menu demarrer/" "Doc export rapport/" "Doc_Lead_Balance/" "Doc_Etat_Fin/" "Doc papier de travail javascript/" "Doc Systeme persistance chat/"
git commit -m "V20 - 30 Août 2026 - Partie 4: Documentation principale"
git push origin main

git add *.md *.txt "Doc_Github_Issue/" "Doc Koyeb deploy/" "Doc backend github/" "deploiement-netlify/"
git commit -m "V20 - 30 Août 2026 - Partie 5: Documentations diverses"
git push origin main

git add .
git commit -m "V20 - 30 Août 2026 - Partie 6: Configuration et fichiers divers"
git push origin main

=================================================================
   COMMANDES DE VÉRIFICATION
=================================================================

# Vérifier l'état Git
git status

# Vérifier le remote
git remote -v

# Vérifier les commits récents
git log --oneline -6

# Vérifier la branche actuelle
git branch --show-current

# Vérifier la connexion au repository
git ls-remote origin

=================================================================
   COMMANDES DE DÉPANNAGE
=================================================================

# Si erreur "nothing to commit"
git status
# Vérifier qu'il y a des fichiers modifiés

# Si erreur d'authentification
# Utiliser un token GitHub comme mot de passe
# Générer sur: https://github.com/settings/tokens

# Si timeout HTTP
# Attendre 2 minutes puis réessayer
Start-Sleep -Seconds 120

# Si le repository n'existe pas encore
# Il sera créé automatiquement au premier push
# Ou créer manuellement sur GitHub:
# https://github.com/new

# Annuler le dernier commit (sans perdre les fichiers)
git reset --soft HEAD~1

# Forcer le rechargement de la config Git
git config --global --unset http.postBuffer
git config --global --unset http.lowSpeedLimit
git config --global --unset http.lowSpeedTime

=================================================================
   MÉTHODE 3: GITHUB DESKTOP (ALTERNATIVE SIMPLE)
=================================================================

1. Télécharger GitHub Desktop: https://desktop.github.com/
2. Ouvrir le projet: File > Add Local Repository
3. Sélectionner: H:\Claverse_1
4. Commit tous les fichiers
5. Publish repository (ou Push si déjà publié)

GitHub Desktop gère automatiquement les gros projets !

=================================================================
   INFORMATIONS IMPORTANTES
=================================================================

✅ Le script divise automatiquement en 6 parties < 30 MB
✅ Retry automatique (3 tentatives) en cas de timeout
✅ Configuration Git optimale pour gros projets
✅ Pas de perte de données - tout est sauvegardé
✅ Peut être relancé sans problème

⚠️ Durée estimée: 10-15 minutes
⚠️ Ne pas fermer PowerShell pendant l'exécution
⚠️ Taille du projet: ~140 MB

📚 Documentation complète:
   - GUIDE_PUSH_V20_30_AOUT_2026.md
   - Doc_Github_Issue/SOLUTION_PROJET_140MB_16_AVRIL_2026.md

🌐 Lien repository:
   https://github.com/sekadalle2024/https-github.com-sekadalle2024-v_20_30-08-2026_persistance_table_isolation_chat_OK

=================================================================
   PROCHAINES ÉTAPES APRÈS LE PUSH
=================================================================

1. Vérifier le repository sur GitHub
2. Configurer la visibilité (public/privé)
3. Ajouter une description au repository
4. Vérifier que tous les fichiers sont présents
5. Partager le lien du repository

=================================================================
